{
  "name": "payment-processor-lambda",
  "version": "1.0.0",
  "description": "Lambda function to process payment events from SQS",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp -r dist/payment-processor/* ./ && rm -rf dist",
    "package": "npm run build && zip -r function.zip index.js"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@temporalio/client": "^1.11.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}

